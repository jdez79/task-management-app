// src/pages/Login.tsx
import React from "react";

const Login: React.FC = () => {
  return (
    <div className="card">
      <h2>Login</h2>
      <p>Use the login button in the navbar to sign in with Auth0.</p>
    </div>
  );
};

export default Login;
